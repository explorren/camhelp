
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/camhelp
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  profiles:
    active: dev
server:
  context-path: /camhelp

